import { OverlapDetailingContent } from "@/components/overlap-detailing";

/** Overlap Detailing — 2 scroll-linked panels (Phase 1.4). */
export function OverlapDetailing() {
  return <OverlapDetailingContent />;
}
