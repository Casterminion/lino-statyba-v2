{
  "name": "lino-statyba",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix site",
    "build": "npm run build --prefix site",
    "start": "npm run start:next --prefix site",
    "lint": "npm run lint --prefix site",
    "verify:rate-limit:prod": "npm run verify:rate-limit:prod --prefix site"
  },
  "dependencies": {
    "vercel": "^54.14.0"
  }
}
